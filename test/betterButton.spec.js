$var chai = require('chai');
$var expect = chai.expect;
$chai.should();
$var betterButton = require('./../index.js');
//In your index file, it would be exports.module = pugLatin(); or just pugLatin
//$function pugLatin() {}
